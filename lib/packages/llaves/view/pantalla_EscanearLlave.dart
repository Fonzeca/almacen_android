import 'package:flutter/material.dart';

class ScanLlaves extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Buscar llave por QR"),);
  }

}